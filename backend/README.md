# Mondaily Backend

Separate Node.js/Express backend shell for Mondaily.

## Local Setup

```bash
cd backend
npm install
npm run dev
```

Test routes:

- `GET /`
- `GET /health`

## Phase

Phase 1, Step 1 only: basic server shell. No database schema or auth logic yet.
