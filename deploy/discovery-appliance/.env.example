# Copy to `.env` (same folder) before running docker compose.
# The bearer token the appliance requires AND that you set as SOVEREIGN_SEARCH_KEY in Vercel.
# Generate one:  openssl rand -hex 32
APPLIANCE_TOKEN=replace_with_a_long_random_token
